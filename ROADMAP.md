# VOID / NARRATIVE 开发路线图

## P0 — 立即修复（影响正常使用）

### 1. 修复 localStorage 数据丢失 Bug
- **文件** `horror-game.html` 第1255行
- `Store.init()` 中 `location.search.match(/load/)` 导致普通打开不加载存档，每次刷新都重置数据
- 改为始终加载，去掉 `?load` 条件判断

### 2. 修复重复 CSS 规则
- 第91-92行 `.dock-input:disabled` 定义了两次 opacity，删除多余的一条

### 3. AI 响应中新角色名误判
- 第1517-1531行的自动创建角色逻辑过于激进
- 加限制：新角色名必须在已知角色名列表中，或明确匹配特定模式才自动创建；否则归入旁白

### 4. 消息 ID 冲突
- `Date.now()` 改 `Date.now() + '_' + Math.random().toString(36).slice(2,6)`

---

## P1 — 补全已有半成品功能

### 5. 论坛/地图/属性数据接入游戏状态
- 目前三块全部用 `DEMO` 静态数据
- 论坛帖从 `Store._db.terminalMessages` 读取，AI 可在响应中通过标记生成帖子
- 地图节点从 `Store._db.currentCopy` 的探索记录动态生成
- 属性面板读取玩家实际数值

### 6. 数据导出/导入 UI
- `Store.exportJSON/importJSON` 已有，但缺少触发入口
- 在 Me 页或设置中加入"导出存档"和"导入存档"按钮

### 7. API 配置弹窗加关闭按钮
- 目前只能点背景关闭，加一个 × 或完成按钮

### 8. 背包显示接入真实数据
- Me 页和用户面板的背包目前是硬编码 HTML
- 改为从 `Store.inventory()` 动态渲染（用户面板已做，Me 页未做）

---

## P2 — 核心游戏架构（副本系统）

### 9. 世界观预设系统 (WorldPresets)
- 预设世界观模板列表（404号公寓、午夜地铁、镜面怪谈等）
- 每个预设包含：核心执念、初始规则提示、系统提示风格、时间设定
- 新建副本时选择世界观

### 10. 副本管理 (Plot 系统)
- 副本存档列表页（替代现在的硬编码消息）
- 新建副本 → 选世界观 → 生成初始场景
- 副本状态追踪：当前章节、位置、任务列表、探索进度
- 切换/删除副本

### 11. 设定库 (LoreCenter)
- 四种设定类型：大世界(global)、局部(local)、角色卡(char)、故事线(storyline)
- CRUD 操作，富文本编辑
- 副本创建时引用设定

### 12. 角色生成
- AI 根据世界观自动生成 NPC 角色卡
- 或从设定库中选择已有角色
- 批量导入到当前副本

---

## P3 — 记忆与持久叙事

### 13. 记忆系统 (MemViewer)
- 当前副本跨对话的记忆聚合：核心记忆、原始对话、副本节点、摘要
- 搜索和按类型筛选
- AI 自动提取关键记忆（san 变化事件、重要发现）
- 记忆压缩：旧记忆自动摘要

### 14. 全局记忆库
- 跨副本的记忆存储
- 在 system prompt 中注入相关历史记忆
- 让 AI 能在不同副本间建立联系

### 15. 日记/摘要自动生成
- 每次对话结束后 AI 自动生成摘要
- 可选：每天/每章自动生成日记条目

---

## P4 — 高级功能

### 16. 多 API 配置
- 主 API（对话）、记忆 API（摘要提取）、状态 API（规则判定）三套独立配置
- 允许不同任务使用不同模型（如对话用大模型，提取用小模型）

### 17. 面具系统 (Masks)
- 玩家可切换身份面具
- 每个面具：名称、性别、身份、外观描述、初始状态
- AI 根据当前面具调整叙事视角

### 18. 流式输出
- AI 响应从 fetch 改为 streaming (`ReadableStream`)
- 实时逐字输出，提升沉浸感

### 19. 存档管理完善
- 多存档槽位
- 存档跨设备迁移（JSON 文件导入导出已就绪，缺多槽位 UI）

### 20. 终端系统完善
- 论坛发帖/回复（AI 模拟其他玩家）
- 排名动态更新

---

## 优先级逻辑说明

- **P0**：不改就没法正常玩 / 会丢数据
- **P1**：现有代码已经搭了架子，补完投入产出比最高
- **P2**：让这个项目从"AI聊天框"变成"无限流文字游戏"的核心架构
- **P3**：让叙事有连续性，长线游玩体验的关键
- **P4**：锦上添花，不影响核心循环
